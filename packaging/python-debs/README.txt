Необязательный запасной путь: .deb python3 и зависимости.

Обычная флешка уже содержит автономный архив в python-linux/.
Сюда кладут .deb только если архив не подошёл (очень старый glibc)
и системного python3 нет. На сервере install.sh сделает dpkg -i *.deb,
без apt и без сети.
