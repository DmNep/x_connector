Кэш автономного Linux CPython для флешки.

py -m xconn_channel stick скачивает сюда архивы
python-build-standalone (CPython 3.12, x86_64 и aarch64) и копирует
их на носитель. На сервере install.sh распаковывает нужный архив в
/opt/x_connector/python. Сами .tar.gz в git не кладутся.
