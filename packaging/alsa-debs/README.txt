alsa-utils (aplay, arecord, amixer) для сервера без сети.

stick копирует сюда .deb jammy (22.04), noble (24.04) и resolute (26.04).
install.sh ставит каталог под VERSION_CODENAME через dpkg -i, без apt.
Сами .deb в git не кладутся.
